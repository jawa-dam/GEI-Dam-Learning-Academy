GEI DAM ACADEMY — AUDIO SYNC PACKAGE

This package contains:
• index.html — patched dashboard with the visible sound control
• js/gei-academy-audio-control.js — shared Academy-wide sound preference
• js/gei-academy-audio-loader.js — shared audio loader

The sound preference is stored at:
gei-academy-sound-enabled-v1

The dashboard patch preserves the supplied 516-line dashboard file and
adds only the audio control wiring requested.

For a new repository, keep the folder structure exactly as packaged.
