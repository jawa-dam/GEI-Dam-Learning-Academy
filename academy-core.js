(()=>{const KEY='geiAcademyMilestone1';const defaults={name:'',xp:0,level:1,completedDays:[],theme:'light',skin:'ocean'};let state={...defaults,...JSON.parse(localStorage.getItem(KEY)||'{}')};const save=()=>localStorage.setItem(KEY,JSON.stringify(state));const $=id=>document.getElementById(id);
function sound(type){try{const C=window.AudioContext||window.webkitAudioContext;if(!C)return;const c=new C(),o=c.createOscillator(),g=c.createGain();const f={click:330,correct:660,wrong:180,xp:880,complete:1047}[type]||330;o.frequency.value=f;o.type=type==='wrong'?'sawtooth':'sine';g.gain.setValueAtTime(.0001,c.currentTime);g.gain.exponentialRampToValueAtTime(.12,c.currentTime+.01);g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+.18);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+.2)}catch(e){}}
function render(){document.body.classList.toggle('dark',state.theme==='dark');document.body.classList.remove('skin-ocean','skin-sunset');document.body.classList.add('skin-'+state.skin);const p=$('points'),l=$('level'),d=$('days'),f=$('xpFill'),t=$('progressText');if(p)p.textContent=state.xp;if(l)l.textContent=state.level;if(d)d.textContent=state.completedDays.length+' / 6';if(f)f.style.width=Math.min(100,state.xp/150*100)+'%';if(t)t.textContent=Math.min(150,state.xp)+' / 150 XP';document.querySelectorAll('.road').forEach(r=>{if(state.completedDays.includes(Number(r.dataset.day)))r.classList.add('complete')});if($('username'))$('username').value=state.name;if($('identityMessage'))$('identityMessage').innerHTML=state.name?'<h2>👋 Welcome, '+escapeHtml(state.name)+'!</h2><p>🎉 You are now in the Academy.</p>':'<h2>👋 Welcome, Explorer!</h2><p>Choose a username before you enter the Academy.</p>';if($('lockedNote'))$('lockedNote').classList.toggle('hidden',!!state.name);if($('enterLevel1'))$('enterLevel1').disabled=false;if($('playerName'))$('playerName').textContent=state.name||'Explorer'}
function escapeHtml(s){return s.replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
document.addEventListener('click',e=>{if(e.target.id==='themeBtn'){state.theme=state.theme==='dark'?'light':'dark';save();render();sound('click')}if(e.target.id==='skinBtn'){state.skin=state.skin==='ocean'?'sunset':'ocean';save();render();sound('click')}});
if($('saveName'))$('saveName').onclick=()=>{const n=$('username').value.trim();if(!n){$('nameNotice').textContent='Please enter a username before starting.';$('nameNotice').classList.remove('hidden');sound('wrong');return}state.name=n;save();$('nameNotice').textContent='🎉 Name saved! Welcome to the Academy, '+n+'!';$('nameNotice').classList.remove('hidden');sound('correct');render()};
if($('enterLevel1'))$('enterLevel1').onclick=()=>{if(!state.name){$('nameNotice').textContent='Please save your username first.';$('nameNotice').classList.remove('hidden');$('username').focus();sound('wrong');return}location.href='level-1.html'};
if($('homeBtn'))$('homeBtn').onclick=()=>location.href='index.html';render();window.GEI={state,save,render,sound};})();